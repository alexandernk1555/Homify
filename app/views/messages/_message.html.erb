<div id="message-<%= message.id %>" class="chat_message">
  <small>
    <strong><%= message.user.first_name.downcase %></strong>
    <i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </small>
  <p><%= message.content %></p>
</div>
